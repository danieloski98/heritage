import { AdminrolecheckGuard } from './adminrolecheck.guard';

describe('AdminrolecheckGuard', () => {
  it('should be defined', () => {
    expect(new AdminrolecheckGuard()).toBeDefined();
  });
});
