import { EditPermissionGuard } from './edit-permission.guard';

describe('EditPermissionGuard', () => {
  it('should be defined', () => {
    expect(new EditPermissionGuard()).toBeDefined();
  });
});
